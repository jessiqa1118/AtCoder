#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
    int N;
    cin >> N;

    for (int i = 0; i < N; i++) {
        ll a, b;
        cin >> a >> b;
        cout << a + b << endl;
    }
}